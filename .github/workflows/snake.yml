name: Contribution snake

on:
  schedule:
    # Every day at 04:17 UTC (01:17 BRT). Off the hour to avoid GitHub's peak-load queue.
    - cron: "17 4 * * *"
  workflow_dispatch:
  push:
    branches: [main]

permissions:
  contents: write

jobs:
  generate:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      # svg-only is the lighter variant of Platane/snk: no gif encoding step.
      - name: Generate snake from the contribution grid
        uses: Platane/snk/svg-only@v3
        with:
          github_user_name: ${{ github.repository_owner }}
          # color_snake matches the accent used in assets/hero-*.svg, so the
          # snake and the hero read as one palette instead of two.
          outputs: |
            dist/snake-light.svg?palette=github-light&color_snake=#0969da
            dist/snake-dark.svg?palette=github-dark&color_snake=#58a6ff

      # Published to the `output` branch so main stays clean of generated assets.
      - name: Push to the output branch
        uses: crazy-max/ghaction-github-pages@v5
        with:
          target_branch: output
          build_dir: dist
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
